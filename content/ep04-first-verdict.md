# EP04 — "Your first verdict in 5 minutes" (onboarding converter)

Runtime target: ~90 s tutorial. Screen-recorded walkthrough with voiceover;
beats are chapters, not pipeline scenes. Goal: viewer ends with a local
verdict on their own machine and (ideally) a referral signup.

## 0:00 hook — 10 s

> From zero to your first protocol verdict in five minutes. No account, no
> deposit, no bet — everything in this video is read-only until YOU decide
> otherwise.

## 0:10 install — 30 s

> Install Python three-ten or newer, then one command: `pip install
> duel-api`. Prove it worked: `duel-api doctor`. Green across the board
> means your machine can run every offline tier.

On screen: real terminal, real output. If `doctor` flags anything, pause
and fix it on camera — troubleshooting builds trust.

## 0:40 demo — 20 s

> Meet the engine with no account at all: `duel-api demo`. Ten thousand
> seeded sessions, right on your machine. This is the same math the live
> protocol uses to say yes — or, as you'll see, no.

## 1:00 verdict — 20 s

> Now the protocol itself: `next_round.py` reads the goal state and names
> only runnable commands — or halts with reasons. Entry affordability,
> floor safety, exposure as the SUM. Nothing hidden, nothing averaged away.

Show a HALT on camera. Say out loud: this is the feature.

## 1:20 cta — 10 s

> Full walkthrough and the affiliate link are below — sign up through it
> and the channel earns a commission at no cost to you. Next week: what a
> real micro bankroll looks like across seventy rounds.

UTM: `?utm_source=youtube&utm_medium=video&utm_campaign=ep04`.
Pin the install command + ToS warning as the top comment.
